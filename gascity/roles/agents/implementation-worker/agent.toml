description = "Implementation worker for source changes, verification, CI repair, and remediation"
# Unscoped (no scope field): instantiated in BOTH the city and rig expansion
# contexts, so city-scope beads (city config, city-owned repos) route to the
# same worker role as rig beads — one worker pattern, no separate city pool
# (operator decision, fernanja gc city 2026-08-20).
fallback = true
